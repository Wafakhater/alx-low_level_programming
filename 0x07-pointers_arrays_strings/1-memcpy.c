#include "main.h"
/**
 * *_memcpy - copise memory area
 * @dest: memory area
 * @src: source
 * @n: lenght of src to be copied
 * Return: the pointer to dest.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
unsigend int i;
for (i = 0; i < n; n; i++)
{
dest[i] = s[i];
}
return (dest);
}
